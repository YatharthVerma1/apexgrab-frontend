# Backend API URL (required for production when frontend is on Vercel and backend on Railway)
# Leave empty for local dev (Vite proxy will forward to localhost:8000)
# Example: https://your-app-name.up.railway.app
VITE_API_URL=

# Optional: Gemini API key for YouTube Summarizer (can also be set only on backend/Railway)
# GEMINI_API_KEY=your_key_here
